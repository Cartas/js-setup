"use strict";
const React = require("react");

const Row = React.createClass({
  render() {
    return (
      <div>
        <span>What</span>
        <a>Click me</a>
      </div>
    );
  }
});

export default Row;
